{
  "name": "@zapizapi/persistence",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "db:push": "drizzle-kit push --force"
  },
  "dependencies": {
    "@zapizapi/application": "workspace:*",
    "@zapizapi/domain": "workspace:*",
    "drizzle-orm": "^0.44.0",
    "postgres": "^3.4.7"
  },
  "devDependencies": {
    "drizzle-kit": "^0.31.4",
    "vitest": "^3.0.0"
  }
}
